import { Link } from "react-router-dom";

export default function ManageContent () {
  return (
    <div className="flex flex-col items-center justify-start w-full">
      <Link to="#">정규훈련 Reset</Link>
    </div>
  );
}